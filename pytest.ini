[pytest]
testpaths = tests
pythonpath = src .
markers =
    model: gerçek Foundry Local modeli gerektirir (yavaş). Varsayılan olarak atlanır;
        çalıştırmak için: FOUNDRY_RAG_E2E=1 pytest -m model
